export type ParcelInfo = {
  area: string;
  category: string;
  shape: google.maps.LatLngLiteral[];
  ownership: string;
  ownershipcode: string;
  ppoint: string;
  purpose: string;
  purposeCode: string;
  unitArea: string;
  use: string;
  zoomTo: string;
};
